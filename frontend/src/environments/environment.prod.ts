export const environment = {
    production: true,
    apiUrl: 'https://your-production-api-url.com/api' , // Your production API URL
    wsUrl: 'ws://localhost:3000'// Adjust the port if your backend runs on a different port
  };